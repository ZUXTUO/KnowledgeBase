# 创建1s空音频

一个简单的Python脚本，用于生成1秒的静音WAV音频文件。

## 功能

- 生成1秒静音音频（默认参数：44.1kHz采样率，单声道，16位）
- 可自定义音频参数（路径、时长、采样率、声道数、位宽）

## 使用方法

```bash
python3 non_audio.py
```

运行后将在当前目录生成 `silence.wav` 文件。

## 自定义参数

可在脚本中修改参数以生成不同规格的静音音频文件。